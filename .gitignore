# Pro tier — private until the paid product launches; never commit publicly
pro/

# Brainstorm / design records (keep local, don't ship in OSS)
.brainstorm/

# OS / editor noise
.DS_Store
*.swp
*.bak
*.tmp

# Local-only test installs
test-install/

# Trial-run reference file — not part of the project
MBSE_Datasheet_Arcadia.pdf

# Claude Code session helper + local-only install artifacts
resume-session
.claude/

# GSD / context-mode tooling state — local only, not part of the project
.planning/
