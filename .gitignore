# Pro tier — private until the paid product launches
pro/

# Brainstorm / design records 
.brainstorm/

# OS / editor noise
.DS_Store
*.swp
*.bak
*.tmp

# Local-only test installs
test-install/

# Local-only install artifacts
.claude/

# GSD / context-mode tooling state — local only, not part of the project
.planning/
